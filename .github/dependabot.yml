# Every dependency here is pinned to an exact version, deliberately: a tool
# or library release must never turn CI red on an unchanged commit. The cost
# of that choice is that pins go stale silently, and a stale pin is how a
# fixed CVE stays exploitable. Dependabot pays that cost back — it is the
# counterpart to audit.yml, which detects the problem but cannot fix it.
#
# There is no npm entry: the frontend has no package.json on purpose. Bun is
# used only to transpile-check the JSX in CI, and its version is pinned in
# tests.yml.

version: 2
updates:
  # Root: requirements-dev.txt (lint/type toolchain) and
  # requirements-test.txt (coverage toolchain). Both exist precisely so
  # these pins are visible here instead of buried in a workflow's install
  # step, where Dependabot cannot see them.
  - package-ecosystem: pip
    directory: "/"
    schedule:
      interval: weekly
      day: wednesday
      time: "04:30"
      timezone: Etc/UTC
    open-pull-requests-limit: 5
    commit-message:
      prefix: "deps"
    labels:
      - dependencies

  # backend/requirements.txt — what the deployed service installs.
  - package-ecosystem: pip
    directory: "/backend"
    schedule:
      interval: weekly
      day: wednesday
      time: "04:30"
      timezone: Etc/UTC
    open-pull-requests-limit: 5
    commit-message:
      prefix: "deps"
    labels:
      - dependencies

  # The workflows pin every action to a commit SHA with the version in a
  # trailing comment. A SHA pin does not move at all — which is the point,
  # since these jobs run with a repository token — and it is also why this
  # entry is not optional: without Dependabot a SHA pin is simply frozen
  # forever.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: wednesday
      time: "04:30"
      timezone: Etc/UTC
    open-pull-requests-limit: 5
    commit-message:
      prefix: "ci"
    labels:
      - dependencies

  # vendor/gh-widgets is a submodule pinned at a commit, and the test suite
  # imports from it. Same failure mode as a SHA-pinned action: nothing moves
  # it unless something opens the bump.
  - package-ecosystem: gitsubmodule
    directory: "/"
    schedule:
      interval: weekly
      day: wednesday
      time: "04:30"
      timezone: Etc/UTC
    open-pull-requests-limit: 3
    commit-message:
      prefix: "vendor"
    labels:
      - dependencies
