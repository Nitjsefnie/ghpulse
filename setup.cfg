[pycodestyle]
max-line-length = 10000
exclude = .idea, .venv, build, dist
# Existing executable frontend-contract fixtures use JavaScript-shaped
# continuations and slices; E127/E203 are not meaningful for those literals.
ignore = E127, E203, W503

[tool:pytest]
asyncio_default_fixture_loop_scope = function
