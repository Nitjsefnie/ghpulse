[pycodestyle]
max-line-length = 100
exclude = .idea, .venv, build, dist

[tool:pytest]
asyncio_default_fixture_loop_scope = function
markers =
    browser: requires the deterministic Playwright Chromium smoke
